<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlay_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="false"
    android:focusable="false">

    <!-- Tooltip container (added dynamically, appears below button in Z-order) -->
    <FrameLayout
        android:id="@+id/tooltip_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"
        android:focusable="false"
        android:visibility="gone" />

    <!-- Button (appears above tooltip in Z-order due to declaration order) -->
    <ImageView
        android:id="@+id/floating_dot"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|start"
        android:background="@drawable/floating_dot_background"
        android:clickable="true"
        android:focusable="false"
        android:visibility="visible"
        android:scaleType="centerInside"
        android:padding="0dp"
        android:contentDescription="@string/app_name" />

</FrameLayout>
